<h1>Usuários Cadastros:</h1>
<div class="container">
    {{#each users}}
        <div class="item">
            <h1>{{this.name}}</h1>
            <p>{{this.occupation}}</p>
            <div class="actions">
                <a href="/users/{{this.id}}">Ver detalhes</a>
            </div>
        </div>
    {{/each}}
</div>
